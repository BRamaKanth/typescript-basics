import {Exportclass} from './17Modulesexport'
//importing class from the path 

class importClass extends Exportclass{
    call(){
        return "";
    }
}

//if we run this class ,exportclass also run -that is the reason u can see name in the console while executing